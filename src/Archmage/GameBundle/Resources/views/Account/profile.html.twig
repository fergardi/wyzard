{% extends "ArchmageGameBundle::layout.html.twig" %}

{% block title %}Perfil{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-xs-12">
            <div class="page-header text-center">
                <h1><i class="fa fa-fw fa-trophy"></i>Perfil</h1>
                {#{% include "ArchmageGameBundle::resources.html.twig" with {'player': player} %}#}
            </div>
        </div>
    </div>
    <div class="jumbotron text-center">
        <div class="row">
            <div class="col-xs-12">
                <div class="box-icon alert alert-{{ player.faction.class }}">
                    <i class="fa fa-fw fa-4x fa-user"></i>
                </div>
                <div class="info">
                    <h1 class="text-center">{{ player.nick }}</h1>
                    <h2><i class="fa fa-fw fa-rocket"></i>{{ player.power|nf }}</h2>
                    <br>
                    {% for achievement in player.achievements %}
                    <div class="col-xs-10 col-xs-offset-1 col-md-4 col-md-offset-{% if loop.index is odd %}1{% else %}2{% endif %} alert alert-{{ player.faction.class }} ribbon text-center text-responsive">
                        <i class="fa fa-fw fa-lg fa-trophy"></i>{{ achievement.name }}
                        <small>({{ achievement.description }})</small>
                    </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}