{% extends "ArchmageGameBundle::layout.html.twig" %}

{% block title %}Mercado{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-xs-12">
            <div class="page-header text-center">
                <h1><i class="fa fa-fw fa-ils"></i>Mercado</h1>
                {% include "ArchmageGameBundle::resources.html.twig" with {'player': player} %}
            </div>
            <div class="col-xs-12 col-md-6 col-md-offset-3">
                <div class="panel panel-default">
                    <div class="panel-heading text-center">Runas</div>
                    <div class="panel-body text-responsive">
                        <div class="row">
                            <div class="col-xs-4">
                                <img class="custom-thumbnail" src="{{ asset('bundles/archmagegame/images/kingdom/rune.jpg') }}">
                            </div>
                            <div class="col-xs-8 text-padded">
                                <div class="col-xs-12">Aquí puedes gastar tus escasas <span class="label label-rune">Runas</span> en comprar los artículos que prefieras de la tienda.</div>
                            </div>
                        </div>
                    </div>
                    <div class="panel-footer text-center">
                        <a class="btn btn-default" href="{{ path( 'archmage_game_kingdom_market' ) }}"><i class="fa fa-fw fa-euro"></i>Comprar</a>
                    </div>
                </div>
            </div>
            {% for rune in runes %}
            <div class="col-xs-12 col-md-6">
                <div class="panel panel-rune">
                    <div class="panel-heading text-center"><i class="fa fa-fw fa-ils"></i><a href="{{ path('archmage_game_home_help') }}#{{ rune.name|slug }}" class="link">{{ rune.name }}</a></div>
                    <div class="panel-body text-responsive">
                        <div class="row">
                            <div class="col-xs-4">
                                <img class="custom-thumbnail" src="{{ asset( rune.image ) }}">
                            </div>
                            <div class="col-xs-8 text-padded">
                                <div class="col-xs-12"><i class="fa fa-fw fa-joomla"></i>{{ rune.skill.description|raw }}</div>
                                <div class="col-xs-12 text-lore"><small><i>{{ rune.lore }}.</i></small></div>
                            </div>
                        </div>
                    </div>
                    <div class="panel-footer text-center">
                        <form id="rune{{ rune.id }}" name="rune{{ rune.id }}" class="form-inline" data-toggle="validator" action="{{ path('archmage_game_kingdom_market') }}" method="post" role="form">
                            <div class="form-group">
                                <div class="input-group">
                                    <div class="btn-group" role="group" aria-label="...">
                                        <button type="submit" class="btn btn-default" {% if rune.cost > player.runes %}disabled{% endif %}><i class="fa fa-fw fa-ils"></i>Gastar {{ rune.cost }} Runas</button>
                                    </div>
                                    <input type="hidden" id="rune" name="rune" value="{{ rune.id }}" required />
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
</div>
{% endblock %}
