{% extends "ArchmageGameBundle::layout.html.twig" %}

{% block title %}Templo{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-xs-12">
            <div class="page-header text-center">
                <h1><i class="fa fa-fw fa-balance-scale"></i>Templo</h1>
                {% include "ArchmageGameBundle::resources.html.twig" with {'player': player} %}
            </div>
        </div>
        <div class="col-xs-12 col-md-offset-3 col-md-6">
            <div class="panel panel-default">
                <div class="panel-heading text-center">Rezar</div>
                <div class="panel-body text-responsive">
                    <div class="row">
                        <div class="col-xs-4">
                            <img class="custom-thumbnail" src="{{ asset('bundles/archmagegame/images/kingdom/temple.jpg') }}">
                        </div>
                        <div class="col-xs-8 text-padded">
                            <div class="col-xs-12">Aquí puedes gastar 10 <span class="label label-extra">Turnos</span> para recibir un encantamiento aleatorio, beneficioso o perjudicial, de uno de los Dioses. Pero cuidado, porque exigirán un sacrificio.</div>
                        </div>
                    </div>
                </div>
                <div class="panel-middle text-center" id="tour-temple">Dioses</div>
                <div class="panel-body text-responsive">
                    {% for god in gods %}
                    <div class="col-xs-6 text-center">
                        <span class="label label-{{ god.faction.class }}"><a href="{{ path('archmage_game_account_profile', {'id': god.id}) }}" class="link">{{ god.nick }}</a></span>
                    </div>
                    {% endfor %}
                </div>
                <div class="panel-footer text-center">
                    <form id="temple" name="temple" class="form-inline" data-toggle="validator" action="{{ path('archmage_game_kingdom_temple') }}" method="post" role="form">
                        <button type="submit" class="btn btn-default"><i class="fa fa-fw fa-balance-scale"></i>Sacrificio</button>
                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('temple') }}">
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}