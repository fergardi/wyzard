{% extends "ArchmageGameBundle::layout.html.twig" %}

{% block title %}Desencantar{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-xs-12">
            <div class="page-header text-center">
                <h1><i class="fa fa-fw fa-chain-broken"></i>Desencantar<br><small><i class="fa fa-fw fa-hourglass"></i>{{ player.turns|nf }}</small></h1>
            </div>
        </div>
        {% for enchantment in player.enchantments %}
        <div class="col-xs-12 col-md-6">
            <div class="panel panel-default">
                <div class="panel-heading text-center"><i class="fa fa-fw fa-lg fa-link"></i><a href="{{ path('archmage_game_home_help') }}#{{ enchantment.spell.name|slug }}">{{ enchantment.spell.name }}</a></div>
                <div class="panel-body text-responsive">
                    <div class="row">
                        <div class="col-xs-4">
                            <img class="custom-thumbnail" src="{{ asset( enchantment.spell.image ) }}">
                        </div>
                        <div class="col-xs-8 text-padded">
                            {{ enchantment.spell.description }}
                        </div>
                    </div>
                </div>
                <div class="panel-footer text-center">
                    <form id="enchantment{{ enchantment.id }}" name="enchantment{{ enchantment.id }}" class="form-inline" action="{{ path('archmage_game_magic_dispell') }}" method="post" role="form">
                        <div class="form-group has-feedback">
                            <div class="input-group">
                                <button type="submit" class="btn btn-default"><i class="fa fa-fw fa-lg fa-chain-broken"></i>Desencantar</button>
                                <input type="hidden" id="enchantment" name="enchantment" value="{{ enchantment.id }}" required />
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
        {% else %}
        <div class="col-xs-12 alert alert-danger text-center" role="alert">
            <i class="fa fa-fw fa-lg fa-exclamation-triangle"></i> Actualmente no tienes encantamientos en tu reino, amigos o enemigos. Tal vez deberías <i class="fa fa-fw fa-lg fa-graduation-cap"></i><a href="{{ path('archmage_game_magic_research') }}">Investigar</a> algunos.
        </div>
        {% endfor %}
    </div>
</div>
{% endblock %}