{% extends "ArchmageGameBundle::layout.html.twig" %}

{% block title %}Explorar{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-xs-12">
            <div class="page-header text-center">
                <h1><i class="fa fa-fw fa-map"></i>Explorar</h1>
                {% include "ArchmageGameBundle::resources.html.twig" with {'player': player} %}
            </div>
        </div>
        <div class="col-xs-12 col-md-offset-3 col-md-6">
            <div class="panel panel-default">
                <div class="panel-heading text-center">{{ player.freePerTurn|nf }} Tierras/Turno</div>
                <div class="panel-body text-responsive">
                    <div class="row">
                        <div class="col-xs-4">
                            <img class="custom-thumbnail" src="{{ asset('bundles/archmagegame/images/kingdom/territory.png') }}">
                        </div>
                        <div class="col-xs-8 text-padded">
                            <div class="col-xs-12">Aquí puedes gastar <span class="label label-extra">Turnos</span> para encontrar más <span class="label label-extra">Tierras</span> libres que añadir a tu reino con las que construir edificios. Cuantas menos tengas, más encontrarás al explorar, y viceversa, hasta 3500.</div>
                        </div>
                    </div>
                </div>
                <div class="panel-middle text-center" id="tour-explore">Fórmula</div>
                <div class="panel-body text-responsive text-only text-center">
                    <p>TIERRAS<sub>exploradas</sub> = (3500 - TIERRAS<sub>jugador</sub>) / 100</p>
                </div>
                <div class="panel-footer text-center">
                    <form class="form-inline" data-toggle="validator" action="{{ path('archmage_game_territory_explore') }}" method="post" role="form">
                        <div class="form-group has-feedback">
                            <div class="input-group">
                                <div class="input-group-btn">
                                    <button type="submit" class="btn btn-default"><i class="fa fa-fw fa-hourglass-half"></i>Explorar</button>
                                </div>
                                <input type="number" class="form-control" id="turns" name="turns" min="1" max="{{ player.turns }}" value="" placeholder="0" required/>
                                <i class="form-control-feedback fa fa-fw" aria-hidden="true"></i>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}